trigger:
  branches:
    include:
    - master
    - refs/tags/*
  paths:
    include:
    - roles/windows_bootstrap/*
    exclude:
    - /**/*.md  
    
pr:
- master

jobs:
  steps:
  - script: echo $(newVersion)
    name: echovar
